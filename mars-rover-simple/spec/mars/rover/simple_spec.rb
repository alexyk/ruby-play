RSpec.describe Mars::Rover::Simple do
  it "has a version number" do
    expect(Mars::Rover::Simple::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
